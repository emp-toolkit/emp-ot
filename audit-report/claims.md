# emp-ot implementation audit — verified claims ledger

**Source tree:** `/Users/wangxiao/Documents/emp-ot` · `HEAD @ 481cc1d` (branch `main`, the 1.0 development line)

**Stats:** 114 merged claims adversarially verified — **96 confirmed, 17 refuted, 1 unverified**.
Produced by 7 subsystem deep-readers (interfaces/streaming, base-classic, base-PQ, IKNP/SoftSpoken,
silent-core, Ferret, sVOLE) + 3 independent design critics; every security-class claim verified through two
independent lenses. Refuted claims are dropped or corrected in place (`corrected_text`).

This file is the refresh anchor for future re-audits. Anchors are repo-relative; note the source lives under the
inner `emp-ot/emp-ot/…` tree while tests live at `test/` (not `emp-ot/test/`). Below is the confirmed,
report-facing subset (the full 114 include lower-value factual confirmations and the refuted set); the two
overview-page refutations are retained here as corrected facts for the record.

---

## Page 00 — Overview

| Verdict | Sev | Claim (corrected where noted) | Anchors | Evidence |
|---|---|---|---|---|
| CONFIRMED | high | No test anywhere drives a deviating party or malformed message; the malicious consistency-check abort paths (mp_gadget chi-fold, IKNP/SoftSpoken tampered-matrix/PPRF, CSW send/recv-check, `Point::from_bin` off-curve, BMM malformed `r`, FTyped/COPE sVOLE checks) are never exercised negatively. `malicious=true` runs are honest-execution correctness only. | `emp-ot/common/mp_gadget.h:592-593,664-665`; `emp-ot/base_ot/csw.h:171,252`; `emp-ot/svole/fp_base_svole.h:261-275`; `test/test.h:14-37`; `test/test_iknp.cpp:22`; `test/test_softspoken.cpp:30`; `test/test_softspoken_lanes.cpp:103`; `test/test_f2k_vole.cpp:45-90`; `test/test_silentferret.cpp` | All 11 test binaries are honest-execution drivers; `error()`/`abort()` are correctness oracles on honest transcripts. Repo-wide grep for corrupt/tamper/malform/deviate/off-curve/bit-flip/FaultIO/EXPECT_DEATH → zero hits. Two independent lenses concur. |
| CONFIRMED | low | Wire format is raw native-endian throughout (blocks, uint64 RO absorptions, whole-`F` `send_data(&x,sizeof(F))`, memcpy'd uint64 SHAKE index inputs); no canonicalization or detection, so cross-endian peers silently diverge. | `emp-ot/common/mp_gadget.h:654,341`; `emp-ot/base_ot/mlkem/bmm.cpp:82` | `io_channel.h`/`ro.h` do raw memcpy; zero htole/htobe/byteswap hits anywhere. Minor cited-line offsets (654 vs 655, 82 vs 84) but the native-endian claim holds. |
| CONFIRMED | low | `trace_hash.cpp`'s protocol table omits SoftSpoken<4> (present in the bench `k∈{2,4,8}` sweep), so a wire-format regression specific to the `k=4` path escapes the documented trace-hash workflow. | `test/trace_hash.cpp:171-184`; `bench/bench_ot_extension.cpp:98-100` | trace_hash traces IKNP, SoftSpoken<2>, SoftSpoken<8>, Ferret, SilentFerret, F2kVOLE, FpVOLE — no `<4>` row. Ferret bootstrap uses SoftSpoken<8> only, so `<4>` isn't exercised indirectly either. |
| CONFIRMED | low | Trace-length is Release/Debug-split: RCOT length `(1<<22)+101` under NDEBUG vs `(1<<18)+101` else (16×); the README baseline is Release-only and will not match Debug-produced hashes. | `test/trace_hash.cpp:121-127`; `docs/wire-trace-hashes.md:118-119` | Confirmed at `trace_hash.cpp:121-127`. Minor anchor nit: the Release/Debug wording is in `trace_hash.cpp` itself, not in `docs/wire-trace-hashes.md`. |
| CONFIRMED | low | Apart from `ideal.h`, the int64 length sweep holds at HEAD: OT/COT/RandomCOT verbs, all four base OTs, StreamingExtension run/next_n/drain_leftover, svole geometry, PRG seek offsets, and PrimalLPN constexpr derivation all carry 64-bit lengths with no realistic overflow surface. | `emp-ot/ot.h:11-12`; `emp-ot/svole/svole.h:44-49`; `emp-ot/common/streaming_extension.h:73-131`; `emp-ot/ot_extension/ferret/silent_ferret.cpp:261`; `emp-ot/tuning.h:35` | All cited sites int64_t; no truncating casts found. Sole exception `ideal.h:22` uses `for(int i…)` vs int64_t length — exactly the claim's carve-out. |
| REFUTED→corrected | med | *Original: the README wire-trace baseline was not re-run this pass.* **Corrected:** the baseline table was independently re-run this pass (Release build, clean HEAD, `EMP_TEST_MODE=1 ./run ./build/trace_hash`); all 18 rows match README byte-for-byte — no drift. | `README.md:136-153`; `test/trace_hash.cpp:98-218` | Rebuilt and ran; every row (CO/CSW/PVW/BMM/IKNP/SoftSpoken<2,8>/Ferret/SilentFerret/F2kVOLE/FpVOLE semi+mali) matches. |
| REFUTED→corrected | low | *Original: `docs/streaming-api.md` stale `do_next` prose + dangling `do_next` mentions.* **Corrected:** core point right (NVI layer removed, `next()` is pure virtual, `do_next` in zero files), but the surviving dangling references are `do_begin` (not `do_next`) at `svole.h:269`, `ferret.h:157/166`, `ferret.cpp:117`; `do_begin` also has zero real definitions. | `docs/streaming-api.md:220-226`; `emp-ot/common/streaming_extension.h:65-67`; `emp-ot/svole/svole.h:269`; `emp-ot/ot_extension/ferret/ferret.h:157` | Grep confirms `next()` pure-virtual and `do_next` absent; the cited locations contain `do_begin`, conflated with `do_next`. |
| REFUTED→corrected | low | *Original: emp-zk's `BoolIO→IOChannel` reinterpret_cast is leftover from emp-ot de-templating `07a6c79`.* **Corrected:** the cast is indeed unnecessary (public non-virtual inheritance), but it predates `07a6c79` (which broke a different subsystem, sVOLE's IO template); origin is emp-zk `e1754ad` (2026-05-15), an earlier Ferret/FerretCOT API change. | `.../emp-zk/emp-zk-bool/zk_bool_base.h:108`; `.../bool_io.h:14` | Cast exists at emp-zk `e1754ad`, 5 days before `07a6c79`; attribution corrected. |

---

## Page 01 — Base OT (CO / PVW / CSW / BMM + ML-KEM)

| Verdict | Sev | Claim | Anchors | Evidence |
|---|---|---|---|---|
| CONFIRMED | med | The CSW `length ≥ 80` precondition (the σ=40 sender-extraction bound `l > 2σ`) is enforced only by `assert()`, elided under NDEBUG, so a release-build caller passing `length<80` silently weakens security with no abort. | `emp-ot/base_ot/csw.h:105,182,46` | Bound is an `assert`; NDEBUG-elided. Verified in code by deep-read + design critic. |
| CONFIRMED | med | The SHAKE-128/256 surface (`symmetric.h`) is reimplemented on OpenSSL EVP and the `gen_matrix` rejection sampler (`crs.hpp`) is reimplemented locally rather than lifted from pq-crystals — the primary non-upstream code needing independent audit against FIPS 202 / the Kyber spec (the `kyber/` arithmetic files stay verbatim). | `emp-ot/base_ot/mlkem/crs.hpp:35-92`; `emp-ot/base_ot/mlkem/symmetric.h:104-198` | Kyber `.c` arithmetic byte-identical to upstream (diff-verified); indcpa/kem/fips202/symmetric-shake/RNG removed and re-implemented on EVP. |
| CONFIRMED | med | On OpenSSL <3.3 the SHAKE-128 squeeze falls back to an O(n²) re-init/re-absorb/finalize-XOF loop returning only the new tail; byte-identity with `EVP_DigestSqueeze` (and cross-version interop) is untested in-tree. | `emp-ot/base_ot/mlkem/symmetric.h:60-62,128-150` | Fallback path present; no in-tree KAT pinning the two code paths equal. |
| CONFIRMED | med | A single CSW instance is stateful across the core/check split (`p0_`/`p1_`/`p_bi_`/`b_copy_`/`length_` are members overwritten each core call), so a CSW object is not safe for concurrent or interleaved OT batches. | `emp-ot/base_ot/csw.h:72,104,181` | Members overwritten per core call; no per-batch isolation. |

*(Trust-model facts confirmed for this page: CO leaves `is_malicious_secure()` at default `false`; PVW `pvw.h:28`, CSW `csw.h:51`, BMM `bmm.h:68` override to `true`; CSW alone overrides `supports_deferred_check()` `csw.h:59`. BMM tightness rests on the RO correlation-breaker with λ=256, `d5aed11`, with no runtime abort.)*

---

## Page 02 — OT extension (interfaces / IKNP / SoftSpoken)

| Verdict | Sev | Claim | Anchors | Evidence |
|---|---|---|---|---|
| CONFIRMED | high | `clone_lane`'s security-critical `lane_salt` contract (nonzero, distinct per lane) — the sole domain separator between concurrent lanes sharing one Δ and one PPRF leaf set — is enforced only by an NDEBUG-elidable assert, and nothing checks for duplicate nonzero salts; a zero/duplicate salt makes the per-chunk AES session key `makeBlock(lane_salt, session)` identical across two lanes sharing leaves and session counters (both start at 0), so the lanes silently emit byte-identical COT streams (Δ/choice reuse), over a 16-bit salt space. | `emp-ot/ot_extension/softspoken/softspoken.cpp:103,477`; `softspoken.h:78,85,121` | Salt is 16-bit, asserted nonzero only; no dedup; session counters both start at 0. |
| CONFIRMED | high | `check_cot`/`check_rot` (test.h), which would exercise `COT::send_cot/recv_cot` and `send_rot/recv_rot`, have zero call sites in any `test/*.cpp` or `bench/*.cpp`, so those two wire code paths are entirely unverified by CI. | `test/test.h:40-61,64-82`; `emp-ot/ot.h:42-43,84-116` | Grep: zero callers of `check_cot`/`check_rot`. |
| CONFIRMED | high | `check_rcot_streaming` (test.h), which exercises `begin()/next()/end()` directly and asserts via `verify_rcot`, has zero call sites, so the streaming contract central to the subsystem is verified for correctness only indirectly (via `run()`'s loop inside `check_rcot`), never directly. | `test/test.h:118-128`; `test/test_iknp.cpp:17`; `test/test_ferret.cpp:17`; `test/test_softspoken.cpp:11,16` | Grep: zero callers of `check_rcot_streaming`. |
| CONFIRMED | med | Only the PPRF check is at bootstrap; the base-OT extraction check is deferred to the parent's first `end()`, and the `setup_done` assert guarantees only that the PPRF check ran. | `emp-ot/ot_extension/ot_extension.h:132,137`; `softspoken.cpp:97,104,110,451-456` | Deferred-check split confirmed; `setup_done` covers PPRF only. |
| CONFIRMED | med | The Δ LSB-must-be-1 invariant is enforced at `set_delta` (assert) and by the ctor pinning `bits[0]=true`; the whole LSB choice convention and RCOT correction depend on it — but the `set_delta` check is NDEBUG-elidable. | `emp-ot/ot_extension/ot_extension.h:79,165`; `emp-ot/ot.h:150` | Assert + ctor pin confirmed; release build unguarded. |
| CONFIRMED | med | The tripwire posture: `~StreamingExtension` mid-session is an always-on `error()`, but enter/exit/assert-in-session are debug-only asserts; the receiver null `gadget_send_` deref path is real. (Nuance: docs describe tripwires as asserts and line 68 says "not enforced by the base", so "advertised as enforced" is loose.) | `emp-ot/ot_extension/ot_extension.h:76`; `streaming_extension.h:133-161,148`; `base_ot/csw.h:105`; `ferret/ferret.cpp:75`; `ferret/silent_ferret.cpp:235`; `softspoken.cpp:102-103`; `docs/streaming-api.md:54-68` | Assert-only vs always-on-`error()` posture fully supported by code. |
| CONFIRMED | med | The `run()`/rcot vs `begin/next_n` mutual exclusion is doc-prose only: `run()` and `next_n()` share one `leftover_` buffer per instance with no assert against mixing, and every chosen-input verb funnels into `rcot→run()→begin()`, so calling a classic OT method on an instance holding a long-lived session (emp-ag's `EMP_AG_LONG_LIVED_COT`, emp-zk's persistent Ferret) double-enters and is caught only by a debug assert. | `emp-ot/common/streaming_extension.h:163-177`; `docs/streaming-api.md:170-173`; `emp-ot/ot.h:45-116`; `ot_extension.h:117-119`; `.../emp-ag/backend/auth_share_pool.h:325-330` | Shared buffer + funnel confirmed; silent draw-ordering corruption under NDEBUG. |

---

## Page 03 — Silent (Ferret / SilentFerret / silent core)

| Verdict | Sev | Claim | Anchors | Evidence |
|---|---|---|---|---|
| CONFIRMED | med | `end()` is the only place `abs_round_` advances (by `n_rounds_`); since the LPN and cGGM-seed PRGs fork from `G = abs_round*t + local`, a `begin()` reusing a non-advanced `abs_round_` repeats those streams — reachable silently only under NDEBUG (the double-begin assert is elided). | `emp-ot/ot_extension/ferret/silent_ferret.cpp:150,87` | Round advance in `end()` only; PRG fork keyed on `abs_round_`. |
| CONFIRMED | med | Mixing `produce_range` with the cursor (`next`/`next_n`/`next_chunks_parallel`) in one prepaid round yields duplicate COTs because `produce_range` is const and doesn't advance the cursor; documented (header:106-109) but unenforced — the nearby assert (cpp:235-237) is a bounds check compiled out under NDEBUG and doesn't detect in-bounds mixing. | `emp-ot/ot_extension/ferret/silent_ferret.h:104`; `silent_ferret.cpp:235` | Const `produce_range`, no cursor advance; assert is bounds-only. |
| CONFIRMED | med | Bit-identity of `prepare_all` to serial `run_next_tree` rests on keeping socket sends and per-tree `get_digest()` snapshots serial on the owning thread in tree order (only pure compute is pooled); correctness-critical and structurally maintained but not runtime-asserted, and no pooled test verifies transcript identity. | `emp-ot/common/mp_gadget.h:196-198,235-240,499-506` | Pooling scope confirmed compute-only; no transcript-identity test. |
| CONFIRMED | med | SilentFerret's correctness depends on `gadget_send_->prg` never being advanced after `cggm_seed_key_` is captured (must use `prepare_all`/`produce_tree`, never `run_next_tree`); a call to `Ferret::run_next_tree` on the sender gadget would diverge the re-derived seeds and produce silently wrong COTs. | `emp-ot/ot_extension/ferret/silent_ferret.cpp:71,78` | Invariant structural, unasserted. |
| CONFIRMED | med | The Fp LPN reduction cadence `kLpnSafeAddsPerReduce=5` must be small enough that `d=10` adds never overflow the Mersenne accumulator before a partial reduce; the value is a per-carrier constant with no runtime bound check, and no test pins it against overflow. | `emp-ot/common/lpn.h:52-65`; `emp-ot/svole/fp_vole.h:78` | Constant unguarded; no overflow test. |
| CONFIRMED | med | `Ferret::bootstrap_` `std::move`s `base_ot` into a temporary nested SoftSpoken instance destroyed on return, leaving cached `csw_base_` dangling at a freed/moved-out CSW for the Ferret's lifetime; never dereferenced today only because Ferret uses the nested instance's deferred check and `set_sid`'s `!setup_done` guard is assert-only — any future code keyed on `csw_base_ != nullptr` (or a release-build post-bootstrap `set_sid`) is a use-after-free/null-deref. | `emp-ot/ot_extension/ferret/ferret.cpp:156-157`; `ot_extension.h:46,102,156` | Move + cached raw pointer confirmed; guard is assert-only. |
| CONFIRMED | med | `next()`/`next_chunks_parallel`'s auto-rollover past the prepaid budget silently performs a full live `end()+begin(batch_n_ots_)` with wire I/O inside a draw the header markets as wire-free; if peers' cumulative draw counts diverge, one side blocks in the reprepay's correction exchange while the other never enters it — a silent distributed hang, no runtime cross-check of draw totals. | `emp-ot/ot_extension/ferret/silent_ferret.cpp:211`; `silent_ferret.h:73` | Auto-rollover does live I/O; no draw-total handshake. |

---

## Page 04 — sVOLE (F2k / Fp)

| Verdict | Sev | Claim | Anchors | Evidence |
|---|---|---|---|---|
| CONFIRMED | high | The Fp path never asserts `set_delta` was called before `begin()`, so a caller who forgets it runs the whole sVOLE with Δ=0 (`delta_value_` default), yielding trivially forgeable MACs. | `emp-ot/svole/fp_vole.h:105`; `svole.h:119,131-136`; `test/test_fp_vole.cpp:55-62` | No Δ-set precondition on the Fp path; default Δ=0. |
| CONFIRMED | med | The inner Ferret gets a derived child sid while the `MultiPointGadget` and pre-stage gadget get the raw `sid.value()`; session separation correctness depends on no concurrent reuse of the raw sid. | `emp-ot/svole/svole.h:208,221-226`; `fp_vole.h:152-153` | Mixed sid derivation confirmed. |
| CONFIRMED | med | F2k `on_set_delta` asserts `Delta.LSB==1`, but the caller constraint is enforced only by an NDEBUG-elidable assert; a release build silently accepts an even Δ. | `emp-ot/svole/f2k_vole.h:93-100` | Assert-only LSB check. |
| CONFIRMED | med | `AuthValueFp` is a plain `{uint64_t val; uint64_t mac;}` (alignof 8, no `alignas(16)`), yet `auth_add_into`/`auth_partial_reduce` reinterpret_cast it to `block*` and deref with aligned `__m128i` semantics — correct only if packed 128-bit with 16-byte placement; the stack local `AuthValue acc` in `Lpn::compute_block` is only 8-byte-guaranteed, so this is formal UB that works by codegen/allocation accident and can fault (movaps/movdqa) at an 8-mod-16 offset. | `emp-ot/svole/fp_vole.h:83-96,25-31`; `emp-ot/common/lpn.h:60` | Struct 8-byte-aligned; aligned-load intrinsics on it. |
| CONFIRMED | med | The `AuthValueF2k`/`AuthValueFp` val-first storage layout is a load-bearing cross-repo ABI (downstream emp-zk depends on val in the low 64 bits of a packed `__uint128_t`) documented only in comments naming the consumer, with no `static_assert` on `sizeof`/`offsetof` to back the raw reinterpret-casts emp-zk performs (`ostriple.h` casts `__uint128_t*` to `AuthValueFp*` on every `next_n` draw); reordering silently corrupts downstream accessors with no compile-time guard. | `emp-ot/svole/f2k_vole.h:26-31`; `fp_vole.h:28-31`; `.../emp-zk/emp-zk-arith/ostriple.h:59`; `docs/adding-a-protocol.md` | Comment-only ABI; downstream cast confirmed; no static_assert. |
